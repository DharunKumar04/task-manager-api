### Project Structure

models/ → Data structures (User, Project, Task)
handlers/ → Route logic (what happens when /tasks is called)
config/ → Database connection setup
routes/ → URL routes definition
middleware/ → Authentication, logging, etc.